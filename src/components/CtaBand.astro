---
interface Props {
  heading: string;
  body: string;
}

const { heading, body } = Astro.props;
---

<section class="cta-band">
  <div class="wrap">
    <h2>{heading}</h2>
    <p>{body}</p>
    <div class="btn-row">
      <slot />
    </div>
    <slot name="under" />
  </div>
</section>
